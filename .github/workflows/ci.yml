name: Quality Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Restore Allure history
        uses: actions/cache/restore@v4
        with:
          path: .allure-history
          key: ${{ runner.os }}-allure-history-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-allure-history-

      - name: Run tests
        run: mvn clean test

      - name: Add Allure history to results
        if: always()
        run: |
          if [ -d .allure-history ]; then
            mkdir -p target/allure-results/history
            cp -R .allure-history/. target/allure-results/history/
          fi

      - name: Generate Allure report
        if: always()
        run: |
          if ls target/allure-results/*-result.json >/dev/null 2>&1; then
            mvn allure:report
          else
            echo "No Allure result files were generated; skipping Allure report."
          fi

      - name: Save Allure history
        if: always() && hashFiles('target/site/allure-report/history/history-trend.json') != ''
        run: |
          rm -rf .allure-history
          mkdir -p .allure-history
          cp -R target/site/allure-report/history/. .allure-history/

      - name: Cache Allure history
        if: always() && hashFiles('.allure-history/history-trend.json') != ''
        uses: actions/cache/save@v4
        with:
          path: .allure-history
          key: ${{ runner.os }}-allure-history-${{ github.run_id }}

      - name: Upload Surefire reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports
          path: target/surefire-reports

      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: target/allure-results

      - name: Upload Allure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: target/site/allure-report
          retention-days: 7

      - name: Prepare Allure report for GitHub Pages
        if: always() && github.event_name == 'push' && hashFiles('target/site/allure-report/index.html') != ''
        run: |
          touch target/site/allure-report/.nojekyll

      - name: Upload GitHub Pages artifact
        if: always() && github.event_name == 'push' && hashFiles('target/site/allure-report/index.html') != ''
        uses: actions/upload-pages-artifact@v3
        with:
          path: target/site/allure-report

  deploy-allure-report:
    if: always() && github.event_name == 'push'
    needs: test
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy Allure report to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
